name: CI - Gateway Service

on:
  push:
    paths:
      - "gtwy-api-service/**"
      - "pom.xml"
  pull_request:
    paths:
      - "gtwy-api-service/**"
      - "pom.xml"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
          cache: maven

      - name: Build Gateway Service
        run: mvn clean install -pl gtwy-api-service -am -DskipTests
